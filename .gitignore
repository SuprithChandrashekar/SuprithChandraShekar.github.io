# Private / personal documents — never publish
uploads/
assets/resume-extracted.txt

# Real PII patterns for scripts/redact-pii.mjs — must NOT leak into the repo
scripts/secrets-patterns.json

# Claude Code local settings
.claude/

# Wrangler (Cloudflare Workers CLI) local cache — contains account email/id
worker/.wrangler/
worker/node_modules/

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/
.vscode/
